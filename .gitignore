node_modules/
screenshots/
scripts/current.pine
temp_*
*.log
discovery-log.json
rules.json

# Python (mt5-bridge)
__pycache__/
*.py[cod]

# MT5 calendar exports (written by calendar_export.mq5)
mt5-bridge/mcp_calendar.json

# Launcher logs
logs/

# Built dashboard bundle — produced by `npm run build` in dashboard-app/.
# The launcher builds it on demand; committing bundles would mean reviewing minified diffs.
mt5-bridge/dashboard/app/

# The journal is personal trading data, not source.
mt5-bridge/journal/

# Order audit log — a record of real trading activity.
mt5-bridge/execution/

# Your own broker→TradingView symbol mappings. Broker naming is per-broker, so
# this is personal configuration rather than something to share.
symbol-map.json
